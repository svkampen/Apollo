cmake_minimum_required(VERSION 2.8)
project(apollo C)
add_subdirectory(src)
add_subdirectory(plugins)
add_subdirectory(hashmap)
set_target_properties(hashmap PROPERTIES LIBRARY_OUTPUT_DIRECTORY ${apollo_SOURCE_DIR}/bin)
